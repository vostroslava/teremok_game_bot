# Teremok Game Bot 🐭

Telegram-бот и веб-приложение для работы с мотивационной моделью «Теремок» и методологией «Формула успешной команды».

## 🚀 Актуальная версия (Web-First)

**Основная архитектура:**
- **aiogram** — Telegram-бот (навигатор, отправляет в WebApp)
- **FastAPI** — веб-приложение (маршруты `/app/*` и `/api/*`)
- **SQLite** — хранение контактов и результатов тестов

**Точки входа:**
- `/app/hub` — главный хаб (лендинг)
- `/app/teremok/*` — раздел «Теремок» (типажи сотрудников)
- `/app/formula/*` — раздел «Формула команды» (системность команд)

> ⚠️ **Папки `legacy/` и `docs/` — архивные версии, не использовать для продакшена.**

## 📦 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/vostroslava/teremok_game_bot.git
cd teremok_game_bot

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # macOS/Linux
# или: venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Скопировать и настроить переменные окружения
cp .env.example .env
# Отредактировать .env — вставить реальные значения
```

## ⚙️ Переменные окружения

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен Telegram-бота от @BotFather |
| `WEB_APP_URL` | Внешний URL (через cloudflared/ngrok) |
| `MANAGER_CHAT_ID` | ID чата менеджера для заявок |
| `REQUIRED_CHANNEL_USERNAME` | Username канала для подписки |
| `CHECK_SUBSCRIPTION_ENABLED` | Включить проверку подписки (true/false) |

## 🏃 Запуск

```bash
# Запустить локально
source venv/bin/activate
python main.py

# В другом терминале — туннель через Cloudflare
cloudflared tunnel --url http://localhost:8000

# Обновить WEB_APP_URL в .env на полученный URL
```

## 📂 Структура проекта

```
teremok_game_bot/
├── main.py              # Точка входа (бот + веб)
├── bot/                 # aiogram хэндлеры и клавиатуры
├── core/                # Конфиг, БД, логика тестов
├── web/
│   ├── routes.py        # FastAPI маршруты
│   ├── static/          # CSS, JS, изображения
│   └── templates/       # Jinja2 шаблоны
├── knowledge_base/      # Материалы по методологиям
├── legacy/              # ⚠️ Архив (не использовать)
└── docs/                # ⚠️ Архив (не использовать)
```

## 🔗 API Эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/check-subscription?user_id=X` | Проверка подписки и наличия контактов |
| POST | `/api/contacts` | Сохранение контактов → уведомление менеджеру |
| POST | `/api/test/submit` | Результат теста → уведомление менеджеру |
| GET | `/api/types` | Список типажей |

## 📝 Лицензия

Материалы предназначены для образовательных и практических целей.
